import 'package:flutter/material.dart';

class LocationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("MapScreen Built");
    return Center(
      child: Text('This Map page is under construction.'),
    );
  }
}
