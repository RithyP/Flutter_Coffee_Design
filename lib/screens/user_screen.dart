import 'package:flutter/material.dart';

class UserScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("UserScreen Built");
    return Center(
      child: Text('This User page is under construction.'),
    );
  }
}
